#include<iostream>
using namespace std;

bool isPrime(int n)
{
    if (n<2)
        return false;
    for (int i = 0; i < n ; i++)
    if (n%2)
    return true;
}
int main()
{
    int fitnessPoint;
    int days;
    cout << "Enter Fitnesspoint:";
    cin >> fitnessPoint;
    cout << "Enter days:";
    cin >> days;

    return 0;
}